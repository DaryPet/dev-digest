# reviewer-core/docs/

Reference docs for `@devdigest/reviewer-core` — how things in this package
work, not what to build (see [`specs/`](../specs/)) or why a decision was made
(see [`INSIGHTS.md`](../INSIGHTS.md)).

Not yet populated — add docs here as the engine grows (e.g. grounding
algorithm, structured-output repair).
