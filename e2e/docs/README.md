# e2e/docs/

Reference docs for `@devdigest/e2e` — how things in this package work, not
what to build (see [`specs/`](../specs/)) or why a decision was made (see
[`INSIGHTS.md`](../INSIGHTS.md)).

Not yet populated — add docs here as the suite grows (e.g. flow authoring
conventions, hermetic-stack internals).
