# client/specs/

Specs for `@devdigest/web` — what a client feature should do, agreed upfront,
before it's built. Source of truth for intended behavior; code should match
it, not the other way around.

Not yet populated — add one spec per feature/lesson as it's designed.
